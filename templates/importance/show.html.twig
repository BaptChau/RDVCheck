{% extends 'base.html.twig' %}

{% block title %}Importance{% endblock %}

{% block body %}
    <h1>Importance</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ importance.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ importance.libelle }}</td>
            </tr>
            <tr>
                <th>Color</th>
                <td>{{ importance.color }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('importance_index') }}">back to list</a>

    <a href="{{ path('importance_edit', {'id': importance.id}) }}">edit</a>

    {{ include('importance/_delete_form.html.twig') }}
{% endblock %}
